# Copier en .env et remplir. NE JAMAIS commiter .env.

# Anthropic (Claude Agent SDK)
ANTHROPIC_API_KEY=

# FullEnrich (REST). MCP = OAuth, pas de cle.
FULLENRICH_API_KEY=

# Sillage : MCP en OAuth (pas de cle). Cle REST optionnelle (sk_live_...).
SILLAGE_API_KEY=

# Folk (CRM warm)
FOLK_API_KEY=

# Microsoft Graph via NextAuth (Azure AD app)
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Google Maps (geocode / places)
GOOGLE_MAPS_API_KEY=

# Gradium (TTS fiche audio) - code promo GTM-HACK
GRADIUM_API_KEY=
