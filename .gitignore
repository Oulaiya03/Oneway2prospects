# deps
node_modules/
web/node_modules/

# env / secrets (JAMAIS commiter)
.env
.env.local
.env*.local
*.key
*.pem

# Sillage : cache de vrais signaux (posts LinkedIn reels -> local only, jamais commite)
data/signals.json

# Gradium : fichiers audio generes (artefacts, local only)
data/*.wav
*.wav

# Folk : base warm CRM REELLE (data interne Mantu -> local only, JAMAIS commite)
data/folk-people.csv
data/*.csv

# next / build
.next/
dist/
build/
out/
.vercel/

# os / logs
.DS_Store
*.log
npm-debug.log*
